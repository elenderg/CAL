To guarantee one row in an text:
  If the text is nil, exit.
  If the text's rows are not empty, exit.
  Create an row given the return byte.
  Append the row to the text's rows.
  Renumber the text's rows.
  
  