To compile some monikettes and some parameters given an rider (definite article):
  If the compiler's abort flag is set, exit.
  Move the rider (compiler rules).
  Scan an name given the rider.
  If the compiler's abort flag is set, exit.
  Prepend "the " to the name.
  Add an monikette to the monikettes given the name.
  
  