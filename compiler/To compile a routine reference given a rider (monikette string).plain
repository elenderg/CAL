To compile an routine reference given an rider (monikette string):
  If the compiler's abort flag is set, exit.
  Add an monikette to the routine reference given the rider's token.
  Move the rider (compiler rules).
  
  