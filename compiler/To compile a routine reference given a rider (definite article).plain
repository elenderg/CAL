To compile an routine reference given an rider (definite article):
  If the compiler's abort flag is set, exit.
  Move the rider (compiler rules).
  Scan an name given the rider.
  Prepend "the " to the name.
  Add an monikette to the routine reference given the name.
  
  