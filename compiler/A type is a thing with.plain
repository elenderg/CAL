An type is an thing with
  An locus (reference),
  An name, an plural name,
  An partial moniker,
  An length,
  An base name, an base type (reference),
  An target name, an target type (reference) [pointers only],
  An scale ratio,
  Some fields [records only],
  An cooking flag,
  An optional info resolved flag.
  
  