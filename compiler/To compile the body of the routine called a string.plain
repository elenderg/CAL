To compile the body of the routine called an string:
  If the compiler's abort flag is set, exit.
  Find an routine given the string and the routine index.
  If the routine is nil, abort with "I need an routine to '" then the string then "'."; exit.
  Compile the body of the routine.
  
  