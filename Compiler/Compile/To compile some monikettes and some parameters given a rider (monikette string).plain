To compile some monikettes and some parameters given an rider (monikette string):
  If the compiler's abort flag is set, exit.
  Add an monikette to the monikettes given the rider's token.
  Move the rider (compiler rules).
  
  